<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3" crossorigin="anonymous">
    <link rel="stylesheet" type="text/css" href="./css/style.css">
    <script src="https://schedule.nylas.com/schedule-editor/v1.0/schedule-editor.js" type="text/javascript"></script>
    <title>FindADoc</title>
</head>

<body>
  <div class="flex-column justify-space-around">
    <header class="display-flex justify-space-between align-center p-2" id="page-header">
      <h1 class="display-4">Find-A-Doc</h1>
      <nav>
        <!-- Conditionally render login or logout links -->
        {{#if logged_in}}
        <a class="nav-link" href="/profile">profile</a> |
        <button class="no-button nav-link" id="logout">logout</button>
        {{else}}
        <a class="nav-link" href="/login">login</a>
        <a class="nav-link" href="/signup">signup</a>
        {{/if}}
      </nav>
    </header>
    <main class="container container-fluid mt-5" id="main-content">
      <!-- Render the sub layout -->
      {{{ body }}}
    </main>
    <footer>
      
    </footer>
  </div>

  <!-- Render script for logged in users only -->
  {{#if logged_in}}
  <script src="/js/logout.js"></script>
  {{/if}}
</body>

</html>
